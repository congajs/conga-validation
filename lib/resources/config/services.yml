parameters:

    conga.validator.constructor: conga-validation:Validator

services:

    validator:
        constructor: "%conga.validator.constructor%"
        tags:
            - { name: kernel.event, event: kernel.compile, method: onKernelCompile, priority: 64 }
